% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_paths.R
\name{build_paths}
\alias{build_paths}
\title{Build multi-path forward selection using AIC}
\usage{
build_paths(
  x,
  y,
  family = c("gaussian", "binomial"),
  K = NULL,
  eps = 1e-06,
  delta = 1,
  L = 50,
  keep_fits = FALSE,
  trace = FALSE
)
}
\arguments{
\item{x}{A \code{data.frame} (matrix) of numerical predictors.}

\item{y}{A \code{vector} of a numerical response for gaussian; 0/1 or factor/logical for binomial.}

\item{family}{A \code{string} containing what family distribution y follows. Must be gaussian or binomial.}

\item{K}{A \code{numeric} maximum number of forward-selection steps (default min(p,10)).}

\item{eps}{A \code{numeric} used to denote the minimum AIC improvement to expand (default 1e-6).}

\item{delta}{A \code{numeric} used to denote the AIC tolerance for near-ties (default 1).}

\item{L}{A \code{numeric} used to denote the max number of models kept per level (default 50).}

\item{keep_fits}{A \code{logical} that determines whether the function saves the model fit objects (default = FALSE).}

\item{trace}{A \code{logical} whether to print light progress messages (default FALSE).}
}
\value{
A \code{list} with elements:
\describe{
\item{path_forest}{list of frontiers (each frontier is a list of model entries)}
\item{aic_by_model}{data.frame of unique models and their AICs}
\item{meta}{parameters and counts}
\item{fits}{named list of fits if keep_fits = TRUE, otherwise NULL}
}
}
\description{
Multi-path forward selection: start from empty model; at each
step add each unused predictor to each current model, keep
near-best children per parent within \code{delta} of the parent's best child,
require improvement >= eps. Deduplicates and cap models per level by L.
}
\examples{
\dontrun{
forest <- build_paths(x = X, y = y, family = "gaussian", K = 6)
forest <- build_paths(x = X, y = y, family = "binomial", K = 6, eps = 1e-2, delta = 2, L = 40, keep_fits = FALSE)
}
}
\author{
Lijuan Wang, Kira Noordwijk, Evan Jerome
}
